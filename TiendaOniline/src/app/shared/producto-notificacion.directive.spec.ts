import { ProductoNotificacionDirective } from './producto-notificacion.directive';

describe('ProductoNotificacionDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductoNotificacionDirective();
    expect(directive).toBeTruthy();
  });
});
